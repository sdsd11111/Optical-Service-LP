{
    "name": "opticaylentes/contacto",
    "description": "Sistema de contacto para Óptica y Lentes",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "phpmailer/phpmailer": "^6.8"
    },
    "autoload": {
        "psr-4": {
            "OpticayLentes\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "platform-check": false
    },
    "scripts": {
        "post-install-cmd": [
            "@composer dump-autoload -o"
        ],
        "post-update-cmd": [
            "@composer dump-autoload -o"
        ]
    }
}
